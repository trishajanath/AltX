# Core FastAPI framework and ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# For data validation and settings management
# Pydantic is a core dependency of FastAPI but we list it for clarity
pydantic==2.7.1
pydantic-settings==2.2.1

# NOTE: fastapi-cors is not needed. CORS is handled by Starlette's
# CORSMiddleware, which is included with FastAPI's standard dependencies.