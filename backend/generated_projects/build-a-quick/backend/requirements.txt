```text
# FastAPI framework and ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Pydantic for data validation and settings management
# pydantic is a core dependency of fastapi
pydantic==2.7.1
pydantic-settings==2.2.1

# Required by FastAPI's CORSMiddleware
# python-cors is not the package, starlette's is built-in
# No separate package is needed, but listing dependencies is good practice.
# FastAPI installs starlette automatically.
```