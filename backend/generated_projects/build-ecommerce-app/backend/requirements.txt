```text
# === Core FastAPI Framework ===
# The main web framework for building the API.
fastapi==0.111.0

# === ASGI Server ===
# High-performance server to run the FastAPI application.
# The [standard] variant includes performance-enhancing libraries like uvloop and httptools.
uvicorn[standard]==0.29.0

# === Data Validation & Settings Management ===
# pydantic is a core dependency of FastAPI but listed here for clarity.
# pydantic-settings is used for type-safe configuration management from environment variables.
pydantic==2.7.1
pydantic-settings==2.2.1

# === Environment Variable Loading ===
# Used by pydantic-settings to load variables from a .env file during local development.
python-dotenv==1.0.1

# SECURITY NOTE: In a production environment, it is critical to regularly scan these dependencies
# for known vulnerabilities using tools like 'pip-audit', Snyk, or GitHub's Dependabot.
# Always pin your dependencies to specific versions to ensure build reproducibility and security.
```